# Spotify API Configuration
# Credentials from: https://developer.spotify.com/dashboard

# Client ID from the application in Spotify
SPOTIFY_CLIENT_ID=your_spotify_client_id_here

# Client Secret (NOT to be included in the Android app code, only in backend)
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# Redirect URI configured in Spotify Dashboard
SPOTIFY_REDIRECT_URI=songswipe://callback

# Package name of the Android application
ANDROID_PACKAGE_NAME=org.ilerna.song_swipe_frontend

# IMPORTANT: 
# - Copy this file as .env and replace the values with your real credentials
# - The .env file will NOT be uploaded to the repository
# - The Client Secret MUST NEVER be in the Android app code
# - It should only be used in the backend to exchange the authorization code for tokens
# - Add the SHA-1 fingerprint of your keystore in the Spotify Dashboard